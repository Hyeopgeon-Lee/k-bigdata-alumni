(function(){'use strict';document.addEventListener('DOMContentLoaded',async()=>{const st=AlumniUI.qs('#home-news-status'),root=AlumniUI.qs('#home-news-list');try{const rows=(await AlumniAPI.request('newsList')).data.slice(0,4);root.innerHTML=rows.map(NewsUI.card).join('')||'<p class="empty">아직 등록된 동문소식이 없습니다.</p>';st.hidden=true}catch(e){AlumniUI.status(st,e.message,'error')}})})();
