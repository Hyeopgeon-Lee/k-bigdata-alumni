(function(){'use strict';const U=AlumniUI;
function dday(job){if(job.always_open)return'상시채용';if(!job.deadline)return'마감일 미정';const today=new Date();today.setHours(0,0,0,0);const due=new Date(job.deadline+'T00:00:00');const days=Math.ceil((due-today)/86400000);return days===0?'오늘 마감':days>0?`D-${days}`:'마감';}
function card(job){const skills=(job.skills||[]).slice(0,4).map(x=>`<span class="chip">${U.esc(x)}</span>`).join('');return `<article class="card job-card"><div class="job-card__flags"><span>${U.esc(job.recruit_type)}</span>${job.referral_available?'<strong>사내추천 가능</strong>':''}</div><h3>${U.esc(job.company)}</h3><p class="job-title">${U.esc(job.job_title)}</p><p class="meta">${U.esc(job.employment_type)} · ${U.esc(job.location||'근무지역 협의')}</p><div class="chips">${skills}</div><div class="job-card__foot"><span>${U.esc(dday(job))}</span><span>${U.esc(job.graduation_year)}년 졸업생 공유</span></div><a class="button" href="job-detail.html?id=${encodeURIComponent(job.job_id)}">자세히 보기</a></article>`}
window.JobUI={dday,card};})();
